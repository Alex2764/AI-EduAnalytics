# 🆓 Настройка на Groq AI (Безплатно!)

Groq предлага **напълно безплатен tier** с много високи rate limits (30 заявки/секунда) - перфектно за този проект!

## Стъпки за настройка:

### 1. Регистрация в Groq
1. Отиди на: https://console.groq.com/
2. Регистрирай се с Google/GitHub акаунт (безплатно)
3. Няма нужда от кредитна карта! 🎉

### 2. Получаване на API ключ
1. След влизане, отиди на: https://console.groq.com/keys
2. Кликни "Create API Key"
3. Копирай генерирания ключ

### 3. Настройка на .env файла

Добави в `backend/.env`:

```bash
# Използвай Groq вместо Gemini
AI_PROVIDER=groq

# Твоят Groq API ключ
GROQ_API_KEY=твоят_groq_api_ключ_тук

# Опционално: избери модел (по подразбиране: llama-3.3-70b-versatile)
# GROQ_MODEL=llama-3.3-70b-versatile
```

### 4. Рестартирай backend сървъра

```bash
# Спри стария сървър
pkill -f "python3.*main.py"

# Стартирай отново
cd backend
python3 main.py
```

## Налични Groq модели:

- `llama-3.3-70b-versatile` (по подразбиране) - Най-добър баланс, най-нов
- `llama-3.1-8b-instant` - По-бърз, по-малък
- `mixtral-8x7b-32768` - Много добър за дълги текстове
- `gemma-7b-it` - Компактен и бърз

## Предимства на Groq:

✅ **Напълно безплатно** - няма нужда от кредитна карта  
✅ **Много високи rate limits** - 30 заявки/секунда  
✅ **Много бърз** - специализирани чипове  
✅ **Добро качество** - използва Llama 3.1 и други модерни модели  
✅ **Лесно за използване** - същия интерфейс като Gemini  

## Превключване обратно към Gemini:

Ако искаш да използваш Gemini отново:

```bash
# В .env файла
AI_PROVIDER=gemini
GEMINI_API_KEY=твоят_gemini_ключ
```

## Помощ:

Ако имаш проблеми:
1. Провери дали API ключът е правилен
2. Провери дали `AI_PROVIDER=groq` е зададен
3. Провери логовете в `/tmp/backend_server.log`

